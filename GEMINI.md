# Git Version Control Rules

- **Git Initialization**: If this project does not have a Git repository initialized, run git init first to initialize it.
- **Commit After Modifications**: After each modification or set of related changes, perform a Git commit with a clear, descriptive commit message to maintain clean version history.
